name: Build Debug APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-debug:
    runs-on: ubuntu-latest
    env:
      JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
      ANDROID_SDK_ROOT: ${{ runner.temp }}/android-sdk
      NODE_OPTIONS: --max-old-space-size=4096

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip curl

      # IMPORTANT: install JS deps BEFORE running Gradle (so node_modules exists for settings.gradle)
      - name: Install JS dependencies (root)
        run: |
          # use yarn if present in repo lockfile, otherwise npm install
          if [ -f yarn.lock ]; then
            yarn install --frozen-lockfile
          else
            npm ci
          fi
        env:
          # increase memory for node if build needs it
          NODE_OPTIONS: --max-old-space-size=4096

      - name: Add node_modules binaries to PATH
        run: |
          echo "${{ github.workspace }}/node_modules/.bin" >> $GITHUB_PATH

      - name: Download Android command line tools (try multiple names)
        run: |
          mkdir -p "$ANDROID_SDK_ROOT"
          cd "$ANDROID_SDK_ROOT"
          FOUND=""
          CANDIDATES=(
            "commandlinetools-linux-13114758_latest.zip"
            "commandlinetools-linux-12266719_latest.zip"
            "commandlinetools-linux-120-9123335_latest.zip"
            "commandlinetools-linux-110-9123335_latest.zip"
            "commandlinetools-linux-9477386_latest.zip"
          )
          for f in "${CANDIDATES[@]}"; do
            url="https://dl.google.com/android/repository/${f}"
            echo "Trying $url"
            code=$(curl -s -o /dev/null -w "%{http_code}" "$url" || echo "000")
            if [ "$code" = "200" ]; then
              FOUND="$f"
              wget -q "$url" -O cmdline.zip
              break
            fi
          done
          if [ -z "$FOUND" ]; then
            echo "Could not find a working commandlinetools zip. Exiting."
            exit 1
          fi
          unzip -q cmdline.zip -d cmdline-tools
          if [ -d cmdline-tools/cmdline-tools ]; then
            mv cmdline-tools/cmdline-tools cmdline-tools/latest
          else
            mkdir -p cmdline-tools/latest
            mv cmdline-tools/* cmdline-tools/latest/ 2>/dev/null || true
          fi
          rm -f cmdline.zip

      - name: Install SDK packages (platform-tools, platform, build-tools)
        run: |
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH"
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --sdk_root="$ANDROID_SDK_ROOT" "platform-tools" "platforms;android-33" "build-tools;33.0.2"
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses --sdk_root="$ANDROID_SDK_ROOT" || true
          ls -la "$ANDROID_SDK_ROOT" || true

      - name: Add Android tools to PATH for rest of job
        run: |
          echo "${ANDROID_SDK_ROOT}/platform-tools" >> $GITHUB_PATH
          echo "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build debug APK
        env:
          ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon

      - name: Upload artifact (debug apk)
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
