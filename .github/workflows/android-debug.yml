name: Build Debug APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-debug:
    runs-on: ubuntu-latest
    env:
      JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
      ANDROID_SDK_ROOT: ${{ runner.temp }}/android-sdk

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          [cite_start]distribution: 'temurin' [cite: 199]
          java-version: '17'

      - name: Install prerequisites
        run: |
          sudo apt-get update [cite: 200]
          sudo apt-get install -y wget unzip unzip

      - name: Download Android command line tools
        run: |
          mkdir -p $ANDROID_SDK_ROOT [cite: 201]
          cd $ANDROID_SDK_ROOT
          echo "Downloading command line tools..."
          # Known working recent filename — change if Google updates it later
          CMDLINE_ZIP="commandlinetools-linux-13114758_latest.zip"
          wget -q "https://dl.google.com/android/repository/${CMDLINE_ZIP}" -O cmdline.zip
          unzip -q cmdline.zip -d cmdline-tools
          # move extracted inner folder to the 'latest' folder expected by sdkmanager [cite: 202]
          if [ -d cmdline-tools/cmdline-tools ]; [cite: 203]
          then
            mv cmdline-tools/cmdline-tools cmdline-tools/latest
          else
            # sometimes zip extracts directly into cmdline-tools/, handle that
            mkdir -p cmdline-tools/latest
            mv cmdline-tools/* cmdline-tools/latest/ 2>/dev/null || true [cite: 204]
          fi
          rm -f cmdline.zip

      - name: Install SDK packages (platform-tools, platform, build-tools)
        run: |
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH" [cite: 205]
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT "platform-tools" "platforms;android-33" "build-tools;33.0.2" [cite: 206]
          # accept licenses
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses --sdk_root=$ANDROID_SDK_ROOT || true [cite: 207]
          echo "PATH after install: $PATH"
          ls -la $ANDROID_SDK_ROOT || true [cite: 208]
          $ANDROID_SDK_ROOT/platform-tools/adb version || true [cite: 209]

      - name: Add Android tools to PATH for rest of job
        shell: bash
        run: |
          echo "ANDROID_SDK_ROOT=${ANDROID_SDK_ROOT}" [cite: 210]
          echo "${ANDROID_SDK_ROOT}/platform-tools" >> $GITHUB_PATH
          echo "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches [cite: 211]
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle- [cite: 212]

      - name: Install JS dependencies
        run: yarn install

      - name: Build debug APK
        env:
          ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
        run: |
          cd android [cite: 213]
          ./gradlew assembleDebug --no-daemon

      - name: Upload artifact (debug apk)
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
